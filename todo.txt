Commands to support
CREATE
- DATABSE <database name>
- TABLE <table name> (<column name> <data type>, <column name> <data type>, ...)
DROP
- DATABASE <database name>
- TABLE <table name>
USE
- <database name>
SELECT
- * FROM <table name>
ALTER
- TABLE <table name> ADD <column name> <data type>
.EXIT


Take user input and parse it into tokens
Check if the first token is a valid command
If it is, check if the rest of the tokens are valid
If they are, execute the command
If not, print an error message
If the first token is not a valid command, print an error message

Most commands require valid tokens first before user-defined names
SELECT will be a sloghtly special case

Create a folder for CREATE DATABASE
Create a file for CREATE TABLE

Each line in a file will be 